# CLI

```bash
tl-guard courses          # list courses
tl-guard preview          # preview Scaffold Map clamp for language/mastery
tl-guard chat             # interactive buddy session
tl-guard demo             # short scripted demo
tl-guard doctor           # check Ollama / LLM connectivity
```

Preview uses the frozen Scaffold Map (not a mutable teacher policy).
